/* ============================================================
   SCRIPT.JS — Main Init That Ties Everything Together
   ============================================================ */

// This file serves as the main entry point that ensures all modules are loaded
// and any cross-module dependencies are properly initialized.

// The individual modules are self-contained and will initialize themselves
// when their script files are loaded.

// Additional cross-module coordination can be added here if needed.

console.log('Portfolio main script loaded - all modules initializing...');